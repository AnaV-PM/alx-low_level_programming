#include "main.h"
/***#### anav-pm ###***/
/**
 * _strcmp - compare string values
 * @s1: input value
 * @s2: input value
 *
 * Return: s1[i] - s2[i]
 */
int _strcmp(char *s1, char *s2)
{
	int anav;

	anav = 0;
	while (s1[anav] != '\0' && s2[i] != '\0')

		if (s1[anav] != s2[anav])

			return (s1[anav] - s2[anav]);

	anav++;

	return (0);
	}
